# ─── Build .NET ───────────────────────────────────────────────
[Bb]in/
[Oo]bj/
publish/
*.user

# Published exe dropped at the root for a manual check (~200 MB): `publish/`
# is ignored, this one was not. A `git add -A` committed it silently and
# GitHub then rejected the push (blob > 100 MB), with a history rewrite as the price.
/cubescope.exe

# ─── Generated SPA (Vite build; embedded on publish via the EmbedSpa target) ───
CubeScope.Server/wwwroot/

# ─── Node / frontend ──────────────────────────────────────────
node_modules/
CubeScope.Web/dist/
CubeScope.Web/*.tsbuildinfo

# ─── Local state (SQLite) ────────────────────────────────────
*.db
*.db-shm
*.db-wal

# ─── IDE / tooling ───────────────────────────────────────────
.vs/
.vscode/
.idea/
.playwright-mcp/

# ─── OS ───────────────────────────────────────────────────────
Thumbs.db
.DS_Store

# ─── Personal tooling (outside the public repo) ──────────────
.clinerules
.github/copilot-instructions.md

# ─── Unredacted internal notes (private, read by Claude Code) ───
CLAUDE.local.md

# ─── Undecided internal docs ─────────────────────────────────
docs/PROJET.md

# ─── Promotion assets (personal, outside the public repo) ────
marketing/

# SDD planning docs (internal, do not publish)
docs/superpowers/
