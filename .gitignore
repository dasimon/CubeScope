# ─── Build .NET ───────────────────────────────────────────────
[Bb]in/
[Oo]bj/
publish/
*.user

# Exe publié déposé à la racine pour une vérification manuelle (~200 Mo) : `publish/`
# est ignoré, celui-là ne l'était pas. Un `git add -A` le committait sans bruit et
# GitHub refusait ensuite le push (blob > 100 Mo), avec réécriture d'historique à la clé.
/cubescope.exe

# ─── SPA générée (build Vite ; embarquée au publish via la cible EmbedSpa) ───
CubeScope.Server/wwwroot/

# ─── Node / frontend ──────────────────────────────────────────
node_modules/
CubeScope.Web/dist/
CubeScope.Web/*.tsbuildinfo

# ─── État local (SQLite) ──────────────────────────────────────
*.db
*.db-shm
*.db-wal

# ─── IDE / outillage ──────────────────────────────────────────
.vs/
.vscode/
.idea/
.playwright-mcp/

# ─── OS ───────────────────────────────────────────────────────
Thumbs.db
.DS_Store

# ─── Outillage perso (hors repo public) ───────────────────────
.clinerules
.github/copilot-instructions.md

# ─── Notes internes non caviardées (privé, lu par Claude Code) ──
CLAUDE.local.md

# ─── Doc interne non tranchée ────────────────────────────────
docs/PROJET.md

# ─── Assets de promotion (perso, hors repo public) ────────────
marketing/

# Docs de planification SDD (internes, ne pas publier)
docs/superpowers/
